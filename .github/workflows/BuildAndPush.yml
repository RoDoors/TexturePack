name: Build And Push
on:
  pull_request:
  push:
    branches:
      - main
  workflow_dispatch:
jobs:
  build-prettify-and-push:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
        with:
          fetch-depth: 0
      - uses: vimtor/action-zip@v1
        with:
          files: assets/ pack.mcmeta pack.png
          dest: ./build/Pack.zip
          recursive: false
      - name: Update hash.txt
        run: |
          sha1sum ./build/Pack.zip > ./build/hash.txt
      - name: Checkout
        uses: actions/checkout@v3
        with:
          ref: ${{ github.head_ref }}
      - name: Prettify code
        uses: creyD/prettier_action@v4.3
        with:
          prettier_options: --write .
      - name: Push
        run: |
          git config advice.addIgnoredFile false
          git config --global user.email "githubactions@github.com"
          git config --global user.name "Github-Actions[bot]"
          git status
          git add -f ./build/Pack.zip
          git add -f ./build/hash.txt
          git commit -m "Update"
          git push origin main
